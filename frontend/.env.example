# Leave this unset for the built-in same-origin proxy to localhost:8000.
# Set it only when the API is hosted separately and has CORS configured.
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com/api/v1
# CHIMERA_API_PROXY_URL=http://localhost:8000
